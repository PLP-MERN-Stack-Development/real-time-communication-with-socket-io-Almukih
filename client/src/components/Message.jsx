import React from 'react'; export default function Message({ m }){ return (<div className='message'><div className='meta'><strong>{m.from}</strong> <small>{new Date(m.ts).toLocaleTimeString()}</small></div><div className='text'>{m.text}</div>{m.reactions && Object.keys(m.reactions).length>0 && (<div className='reactions'>{Object.entries(m.reactions).map(([k,v])=> <span key={k}>{k} {v}</span>)}</div>)}</div>); }